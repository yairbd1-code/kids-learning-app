# kids-learning-app

## על הפרויקט
אפליקציית לימוד לילדים שיוצרת הרגל לימוד יומי דרך מערכת נקודות ותגמולים משפחתית.
המשתמש (בעל הפרויקט) הוא **לא מתכנת** — הוא לומד לבנות מוצר באמצעות AI. תמיד להסביר
ברמה של יזם טכני, לא כמו למורה תכנות, ולא להניח ידע תכנות קודם.

## שלב נוכחי: רובד 1 שלם ✅, רובד 2 התחיל (2026-08-27, בבקשה מפורשת של המשתמש)

**רובד 1 (המנגנון הבסיסי):**
- פרופיל משפחה → הורה (משתמש) → ילד(ים) ✅
- ארנק נקודות לכל ילד ✅
- תנועות נקודות (זיכוי/חיוב) שההורה מבצע ידנית ✅
- חנות פרסים שההורה מגדיר (שם, עלות בנקודות) ✅
- מימוש פרס: ההורה מאשר, המערכת מקזזת נקודות ✅
- מספר ילדים למשפחה אחת ✅ (נבדק בפועל)

**רובד 2 — התחלנו, בהיקף מוגבל ומכוון (ראו "חזון רובד 2" למטה למה שעוד לא נבנה):**
- משימות לימוד שההורה מגדיר ומסמן כבוצעו (עם תגי גיל/נושא) ✅ — ראו סעיף למטה
- **מנוע תרגול אדפטיבי (כיתות א-ח × 3 רמות, PIN לילד, לומדה, מחברת טיוטה) —
  ✅ בנוי קצה-לקצה (DB→API→Flutter), עדיין לא נבדק ויזואלית ע"י המשתמש** —
  ראו סעיף מפורט למטה

⚠️ **חשוב:** גם שרובד 2 התחיל, זה **לא** אישור גורף לבנות הכל מהחזון המלא (ראו
למטה) — רק את מה שכבר סוכם ונבנה בפועל. חלקים גדולים של החזון (למידה
אדפטיבית, OCR) עדיין דורשים שיחת תכנון נפרדת לפני מימוש, כמפורט למטה.

## סטאק טכני
- **Client:** Flutter — אותו קוד ל-Android ול-Web (רספונסיבי). מתחילים ב-Android
  (אין למשתמש מחשב Mac, ולכן אין build ל-iOS בשלב זה).
- **Backend:** Node.js + TypeScript + Express
- **DB:** PostgreSQL
- **Auth:** מומש עצמאית (bcrypt להצפנת סיסמאות + JWT) — לא Firebase Auth/Supabase
  Auth. זו בחירה מודעת של שליטה מלאה, גם במחיר עבודה נוספת.
- **Hosting:** Render או Railway (לא AWS/GCP — מורכבים מדי לשלב זה)
- **Git:** GitHub, ריפו kids-learning-app תחת yairbd1-code

## מבנה תיקיות
הפרויקט יושב ב-`C:\Users\yairb\kids-learning-app` (**לא** תחת "בניית אפליקציות"
— הועבר ב-2026-08-27 כי כלי הבנייה לאנדרואיד לא תומכים בנתיב עם עברית. ראו
היסטוריה בהמשך המסמך).
- `app/` — פרויקט ה-Flutter
- `backend/` — שרת ה-Node/Express

## מודל נתונים

```
Family (משפחה — join_code ייחודי בן 6 תווים לצירוף הורה נוסף)
  └─ ParentUser (הורה, מתחבר עם סיסמה — יכולים להיות כמה לאותה משפחה)
  └─ Child (ילד — שם, גיל, כיתה, pin_hash למצב תרגול,
            disabled_subjects[], subject_weights JSON לתרגול מעורב)
       └─ PointsWallet (ארנק — יתרה נוכחית)
            └─ PointsTransaction (זיכוי/חיוב, תאריך, סיבה)
       └─ ChildSubjectProgress (לכל מקצוע: currentGrade 1-8, currentDifficulty)
       └─ PracticeAttempt (לוג תשובה - נכון/לא, קישור לשאלה ולתנועת נקודות)
       └─ ChildCurriculumNote (חומר לימוד השנה: טקסט, או תיאור שחולץ מתמונה)
  └─ Question[] (שאלות פרטיות שנוצרו ע"י AI/ידנית עבור המשפחה — ראו למטה)
Reward (פרס — שם, עלות בנקודות, שייך למשפחה)
  └─ Redemption (מימוש — קישור לילד, לפרס, לתאריך)
LearningTask (משימת לימוד שההורה מגדיר)
  └─ TaskCompletion (סימון ביצוע לילד ספציפי)
Question (subject, gradeLevel 1-8, difficulty, questionText, options,
  correctOptionIndex, explanation, source SEED/AI/MANUAL, approved,
  familyId אופציונלי — null=מאגר גלובלי, אחרת פרטי למשפחה)
```

## אופן עבודה מוסכם
1. **להסביר לפני שכותבים קוד** — מה קיים היום, איך זה עובד, מה הבעיה/המשימה.
2. **להציע אפשרויות עם המלצה** — לא לקפוץ ישר למימוש. לחכות לאישור.
3. **קוד רק אחרי אישור מפורש**.
4. **דיאגרמות לארכיטקטורה**, הסבר על זרימת הקוד לפני הצגת קוד עצמו.
5. **צעדים קטנים, קצה-לקצה** — עדיף פיצ'ר אחד שלם (DB→API→מסך) על פני הרבה חלקים
   לא גמורים.
6. לתעדף קוד production-quality, maintainable, מאובטח וסביר במחיר — לא את המהירות
   של כתיבתו.
7. לעבוד בעברית ככל האפשר בהסברים.

## מה כבר בנוי — רובד 1 שלם פונקציונלית! 🎉

כל חמשת החלקים שהוגדרו כ-MVP קיימים בקוד ועברו בדיקות אוטומטיות/curl:

- **Auth** — הרשמה/login עם סיסמה (`bcryptjs`+JWT), **וגם** התחברות עם Google
  (קוד מוכן, ה-Client ID האמיתי כבר מוזן — עדיין לא נבדק ויזואלית בדפדפן ע"י
  המשתמש). ראה [auth.ts](backend/src/routes/auth.ts).
- **ילדים+ארנק** — `GET/POST /children`, כל ילד מקבל ארנק אוטומטית. ראה
  [children.ts](backend/src/routes/children.ts).
- **תנועות נקודות (זיכוי/חיוב)** — `GET/POST /children/:childId/transactions`,
  מעדכן את הארנק אטומית, חוסם יתרה שלילית. ראה [transactions.ts](backend/src/routes/transactions.ts).
- **חנות פרסים** — `GET/POST /rewards`, `DELETE /rewards/:id` (חסום אם כבר מומש).
  ראה [rewards.ts](backend/src/routes/rewards.ts).
- **מימוש פרס** — `GET/POST /children/:childId/redemptions`. כל מימוש יוצר גם
  תנועת נקודות (חיוב) אוטומטית, כך שההיסטוריה מסתנכרנת בלי לוגיקה כפולה. ראה
  [redemptions.ts](backend/src/routes/redemptions.ts).

**Flutter** — כל הפיצ'רים משולבים ב-UI: מסך login/הרשמה (כולל Google) →
"הילדים שלי" (עם כפתור לחנות פרסים) → מסך ילד בודד (יתרה, היסטוריית תנועות,
הוספת זיכוי/חיוב, מימוש פרס). `flutter analyze`+`flutter test` נקיים לאורך כל
הדרך.

**סטטוס בדיקות:** backend נבדק ביסודיות עם curl (כולל edge cases: הרשאות בין
משפחות, ולידציות, יתרה שלילית). ה-Auth+"הוספת ילד" הבסיסיים נבדקו ויזואלית
ע"י המשתמש ועבדו. **תנועות נקודות, חנות פרסים, מימוש, ו-Google Sign-In עדיין
לא נבדקו ויזואלית ע"י המשתמש** — ה-agent לא הצליח לצלם/ללחוץ בדפדפן הפנימי
בסביבה הזו לאורך כל השיחה.

**לא בנוי עדיין:** שום דבר מרובד 1. הצעד הבא אחרי בדיקה ויזואלית הוא רובד 2-3
(תוכן לימודי, AI) — **רק אם יתבקש במפורש** — או ליטוש/דיפלוי של רובד 1.

## ✅ נפתר: בעיית נתיב עברית ← אנדרואיד עובד

הפרויקט הועבר מ-`C:\Users\yairb\בניית אפליקציות\kids-learning-app` (עברית בנתיב
= קריסת כלי הבנייה) ל-`C:\Users\yairb\kids-learning-app` (אושר ע"י המשתמש
ב-2026-08-27). הועתק עם robocopy ואומת (diff -rq) שהקוד וה-git history זהים
לגמרי. **בנייה לאנדרואיד רצה בהצלחה מהנתיב החדש**: `flutter run -d
emulator-5554` בנה APK, התקין על אמולטור Pixel 9, והאפליקציה עלתה בלי שגיאות
(Impeller rendering backend, לא נראו exceptions בלוג). זו גם הסיבה שהוספתי
לפני כן ל-[auth_service.dart](app/lib/services/auth_service.dart) זיהוי
פלטפורמה שמפנה לכתובת `10.0.2.2` באנדרואיד במקום `localhost` (אנדרואיד רואה
את המחשב המארח בכתובת מיוחדת הזו).

**נשאר על הדיסק (לא דחוף):** התיקייה הישנה `C:\Users\yairb\בניית אפליקציות\kids-learning-app`
לא נמחקה — קובץ בתוכה נעול ע"י תהליך שלא זוהה (כנראה סורק/אינדקסר). אפשר
למחוק אותה ידנית כשנוח (Restart למחשב יפתור את זה בוודאות אם המחיקה הרגילה
לא עובדת).

**מה עדיין לא נבדק ויזואלית ע"י המשתמש:** ה-agent לא הצליח לצלם/ללחוץ בדפדפן
הפנימי או על האמולטור לאורך כל השיחה הזו (מגבלת סביבה) — כל הבדיקות שנעשו הן
ברמת קוד/רשת/לוגים. **המשתמש אמר שיבדוק בעצמו על האמולטור בהמשך.**

## Google Sign-In באנדרואיד — קוד מוכן, ממתין לצעד שלך ב-Google Cloud

**מה גילינו:** המשתמש דיווח שהתחברות Google לא עבדה באמולטור. הסיבה: כפתור
Google היה מוגבל ל-Web בלבד (`kIsWeb`) — באנדרואיד הוא פשוט לא הופיע. Web כן
עבד (וידאתי ב-DB: `google_id` כבר מקושר לחשבון yairbd1@gmail.com מניסיון קודם
מוצלח).

**מה תיקנתי ב-[login_screen.dart](app/lib/screens/login_screen.dart):**
הסרתי את הגבלת ה-Web, הוספתי `serverClientId` לאתחול (נדרש באנדרואיד כדי
לקבל idToken תקף לאימות מול השרת), והוספתי כפתור טבעי לאנדרואיד (קורא
ל-`GoogleSignIn.instance.authenticate()`, לא כפתור ה-iframe של Web).
`flutter analyze` נקי.

**⚠️ צעד שרק אתה יכול לעשות — Android OAuth Client ב-Google Cloud Console:**
1. **APIs & Services → Credentials → + CREATE CREDENTIALS → OAuth client ID**
2. Application type: **Android**
3. Package name: `com.kidslearningapp.kids_learning_app`
4. SHA-1 (מ-debug keystore, חישבתי בעצמי): `89:4F:49:16:EF:9B:64:BF:A8:F4:5D:C5:25:4B:4F:E7:4E:5A:1B:19`
5. Create — **אין צורך להעתיק את ה-Client ID החדש לשום מקום בקוד**, גוגל מזהה
   את האפליקציה לפי החתימה בלבד; הקוד ממשיך להשתמש ב-Web Client ID הקיים.

**עדכון: המשתמש יצר את ה-Android OAuth Client** (2026-08-27) — נבנה ונבדק
מחדש על האמולטור. עדיין לא אושר ויזואלית שההתחברות בפועל עובדת (agent לא יכול
לראות את המסך).

## תיקון באג: race condition ביתרת נקודות

**מה מצאתי (בבדיקת בעצם, לא דיווח מהמשתמש):** ב-`transactions.ts` וב-
`redemptions.ts`, עדכון היתרה היה "קרא יתרה נוכחית → חשב יתרה חדשה → כתוב".
תחת שתי בקשות בו-זמניות ממש (למשל שני קליקים כמעט יחד, או שני מכשירים), שתיהן
יכלו לקרוא את אותה יתרה ישנה, לעבור את בדיקת "יש מספיק נקודות", ולגרום ליתרה
סופית שגויה (ואפילו שלילית) — למרות ששתי התנועות עצמן נרשמות נכון.

**התיקון:** יצרתי [points.ts](backend/src/points.ts) עם פונקציה שנועלת את שורת
הארנק ב-DB (`SELECT ... FOR UPDATE`) לפני הבדיקה והעדכון, כך שבקשות מקבילות
מסתדרות בתור אחת אחרי השנייה במקום לדרוס אחת את השנייה. **בדקתי בפועל:** יריתי
שתי בקשות חיוב זהות (15 נקודות, יתרה 20) בו-זמנית ממש — אחת הצליחה (20→5),
השנייה נדחתה נכון ("אין מספיק נקודות"), והיתרה הסופית נכונה. `tsc --noEmit`
נקי, כל בדיקות ה-regression עברו.

## עברית מלאה ב-Flutter

הוספתי `flutter_localizations` + `locale: Locale('he')` ב-[main.dart](app/lib/main.dart)
(במקום ה-`Directionality` הידני שהיה קודם) — כך שגם רכיבי Material מובנים
(לא רק הטקסט שלנו) יתנהגו נכון בעברית/RTL, ללא תלות בשפת המכשיר (האמולטור
מוגדר ל-en-US כברירת מחדל). `flutter analyze`+`flutter test` נקיים.

## תיקון באג אבטחה: שגיאות שרת דלפו stack trace

**מה מצאתי:** בקשה פגומה (JSON לא תקין) או route שלא קיים החזירו דף שגיאה
HTML של Express בברירת המחדל, כולל **stack trace מלא עם נתיבי קבצים במחשב**.
זה גם חושף מידע פנימי, וגם היה גורם ל-Flutter לקבל תשובה שהוא לא יודע לפרש
(JSON parse error) במקום הודעת שגיאה ברורה.

**התיקון:** הוספתי ב-[index.ts](backend/src/index.ts) handler ל-404 ו-error
handler גלובלי שמחזירים תמיד JSON נקי (`{"error": "..."}`), ורושמים את
השגיאה המלאה רק ל-log של השרת (לא ללקוח). בדקתי: גם בקשת JSON פגום וגם route
לא קיים מחזירים עכשיו JSON נקי עם קוד סטטוס נכון (400/404).

## אימות: ריבוי ילדים למשפחה עובד

בדקתי בפועל (יצרתי 2 ילדים באותה משפחה, זיכיתי כל אחד בסכום שונה) — היתרות
וההיסטוריה של כל ילד עצמאיות לגמרי, אין דליפה בין ילדים. זה היה סעיף ברשימת
רובד 1 שעדיין לא אומת במפורש.

## הכנה לדיפלוי: Dockerfile

הוספתי [backend/Dockerfile](backend/Dockerfile) (multi-stage build) ו-
[.dockerignore](backend/.dockerignore). הוספתי גם `binaryTargets =
["native", "debian-openssl-3.0.x"]` ל-generator ב-schema.prisma כדי
שה-Prisma client יעבוד גם בקונטיינר לינוקס וגם על המחשב המקומי (Windows).
**לא נבדק בפועל** — אין Docker מותקן על המחשב הזה, ולא התקנתי אותו (התקנה
כבדה, לא דחוף כרגע). כשנגיע לדיפלוי בפועל: migrations (`prisma migrate
deploy`) צריכות לרוץ כצעד build/release נפרד אצל Render/Railway (לא בתוך
ה-Dockerfile של ה-runtime, כי ה-CLI של Prisma לא כלול שם בכוונה כדי לשמור
על האימג' קטן).

## משימות לימוד (רובד 2, חלק ראשון) — בנוי ונבדק

בדיוק כמו חנות פרסים/מימוש, רק הפוך: ההורה מגדיר **משימת לימוד** (שם, נושא
אופציונלי, טווח גיל מומלץ, ניקוד), ומסמן אותה כ"בוצעה" עבור ילד ספציפי —
המערכת מזכה נקודות אוטומטית (יוצרת PointsTransaction, אותו מנגנון קיים).

- **DB:** `LearningTask` (`learning_tasks`) + `TaskCompletion`
  (`task_completions`) — ראו [schema.prisma](backend/prisma/schema.prisma).
- **API:** `GET/POST /learning-tasks`, `DELETE /learning-tasks/:id` (חסום אם
  כבר בוצעה), `GET/POST /children/:childId/task-completions`. ראו
  [learningTasks.ts](backend/src/routes/learningTasks.ts) ו-
  [taskCompletions.ts](backend/src/routes/taskCompletions.ts).
- **Flutter:** מסך [learning_tasks_screen.dart](app/lib/screens/learning_tasks_screen.dart)
  (נגיש מ-AppBar ב"הילדים שלי", אייקון ✓), עם **תבניות ניקוד מוכנות** (קצרה=10,
  בינונית=25, גדולה=50 — ניתן ללחוץ במקום להקליד, בדיוק כמו שהמשתמש ביקש).
  ב-[child_detail_screen.dart](app/lib/screens/child_detail_screen.dart)
  נוסף כפתור "סימון משימה כהושלמה" שמציג משימות **מותאמות לגיל הילד קודם**.
- **נבדק ביסודיות עם curl:** יצירה עם טווח גיל, ולידציה (טווח גיל הפוך נדחה),
  השלמת משימה מזכה נקודות נכון, היסטוריה, חסימת מחיקה של משימה שכבר בוצעה.
  `tsc --noEmit`+`flutter analyze`+`flutter test` נקיים.

## חזון רובד 2 המלא (מהמשתמש, 2026-08-27) — לא נבנה עדיין, דורש שיחת תכנון

המשתמש תיאר חזון מלא בהרבה יותר גדול ממה שנבנה עד כה. **לא להתחיל לבנות את
הסעיפים האלה בלי לחזור ולדבר על ארכיטקטורה קודם:**

1. **התאמת תוכן לגיל הילד** — ✅ חלקית נבנה (טווח גיל על משימה). עדיין אין
   "תוכן לימודי" אמיתי (שאלות, תרגילים) — רק משימות עם שם.
2. **למידה אדפטיבית שמתאימה את עצמה לרמת המשתמש** — **לא נבנה, דורש תכנון
   גדול.** המשתמש הבהיר מפורש: "בזמן התרגול של הילד שלא יהיה מעורבות ההורה —
   הכל נמדד ומיוצר אוטומטית לפי העבודה של הילד". זה אומר:
   - צריך **ממשק לילד עצמו** בתוך האפליקציה (כרגע האפליקציה כולה להורה בלבד,
     אין login/מסך לילד) — שאלה פתוחה: איך ילד "נכנס"? PIN? בחירת פרופיל
     בתוך session ההורה? מכשיר נפרד?
   - צריך תוכן לימודי בפועל עם תשובות נכונות לבדיקה אוטומטית (שאלות רב-ברירה?
     תרגילי חשבון? קריאה בקול שנבדקת?) — מי כותב/מספק את התוכן?
   - צריך מנוע שמתאים קושי אוטומטית לפי ביצועים (למשל: algorithm כמו
     spaced-repetition, או מדרג קושי פשוט) — ומזכה נקודות אוטומטית בלי אישור
     הורה, בניגוד למנגנון הידני שבנינו עד כה.
3. **הורה בוחר נושאים לחיזוק** — ✅ חלקית נבנה (שדה `subject` על משימה, טקסט
   חופשי). עדיין אין "פרופיל חוזק/חולשה" של הילד לפי נושא — צריך נתוני ביצועים
   אמיתיים (תלוי בסעיף 2) כדי שזה יהיה אוטומטי, לא רק תיוג ידני.
4. **התאמה לפי ספרי הלימוד בכיתה, כולל צילום רשימת ספרים (OCR)** — **לא
   נבנה, דורש תכנון גדול.** שאלות פתוחות: איזה שירות OCR/זיהוי תמונה (Google
   Cloud Vision? שירות אחר?), ואיך "שם ספר שזוהה בתמונה" הופך לתוכן לימודי
   בפועל שהמערכת יודעת ללמד ממנו — כרגע אין מאגר תוכן שמקושר לספרי לימוד.
5. **תבניות ניקוד מוכנות (לא לתמחר בעצמו)** — ✅ נבנה גם למשימות לימוד וגם
   לחנות הפרסים (קטן=20/בינוני=50/גדול=100).

## דרישה עתידית: תמיכה בריבוי שפות (עברית + אנגלית)

**מהמשתמש (2026-08-27):** "אני חייב שהאפליקציה תעבוד בעברית (שיהיה אפשר לשנות
לאנגלית) אבל קודם כרגע אני חייב עברית שאוכל לבדוק."

**מצב נוכחי:** כל הטקסט באפליקציה כתוב בעברית ישירות בקוד (לא תלוי בשפת
המכשיר) — זה מה שמוצג כרגע, וזה עונה על הצורך המיידי לבדיקה.

**לא נבנה עדיין:** מנגנון החלפת שפה אמיתי (עברית↔אנגלית) — זה דורש להמיר את
כל הטקסטים למערכת תרגומים (למשל קבצי `.arb` + חבילת `intl`/`flutter_gen`),
ולהוסיף כפתור/הגדרה להחלפת שפה שמשנה גם כיוון טקסט (RTL↔LTR) בו-זמנית. זו
עבודה משמעותית שנוגעת בכל מסך — **לא להתחיל בלי לתאם קודם**, כי זה סוג של
refactor רוחבי שכדאי לתכנן (למשל: להחליט אם בכלל צריך בשלב הזה, כמה מסכים יש
לתרגם, האם יש עוד טקסטים דינמיים מה-DB שצריך גם לחשוב עליהם).

## השלמות CRUD (2026-08-27) — עריכה/מחיקה שהיו חסרות

זיהיתי ותיקנתי כמה פערים אמיתיים: לא הייתה שום דרך לתקן טעות הקלדה או להסיר
פריט שנוסף בטעות.

- **ילדים:** `PATCH/DELETE /children/:id` — עריכה (שם/גיל/כיתה) ומחיקה (עם
  ניקוי מלא של כל ההיסטוריה שלו — תנועות, מימושים, השלמות משימה — בטרנזקציה
  אחת). נגיש דרך תפריט (⋮) במסך הילד הבודד.
- **פרסים ומשימות לימוד:** `PATCH /rewards/:id` ו-`PATCH /learning-tasks/:id`
  — עריכה מלאה. נגיש דרך לחיצה על הפריט ברשימה (לא רק על אייקון המחיקה).
- כל השינויים נבדקו עם curl (כולל מקרי קצה: ולידציה, 404, 400) ועברו
  `flutter analyze`+`flutter test`+`flutter build apk --debug` (בדיקת קומפילציה
  לאנדרואיד, ראו הערה למטה על אמולטור).

## מנוע התרגול (רובד 2, החלק המרכזי) — בנוי קצה-לקצה (DB→API→Flutter)

**החלטות מהשיחה (2026-08-27):**
- כניסת ילד: קוד PIN אישי (4 ספרות) לכל ילד. הילד נכנס "למצב תרגול" **מתוך
  session ההורה הפעיל** (ההורה כבר מחובר במכשיר המשפחתי) — לא login עצמאי מלא.
  ✅ נבנה: `POST /children/:id/pin` (הורה קובע PIN), `POST
  /children/:id/child-session` (מחליף PIN ב-token ילד נפרד, תוקף 12 שעות).
- תוכן: מאגר שאלות **גלובלי** (לא לכל משפחה), רב-ברירה בלבד בשלב הזה. שלושה
  מקצועות ראשונים: אנגלית, חשבון, עברית. AI ליצירת שאלות = **עתידי, פרימיום**
  (לא בונים עכשיו, רק משאירים את הארכיטקטורה פתוחה לזה).
- ניקוד: תשובה נכונה = 3 נקודות, מזוכה אוטומטית (ללא מעורבות הורה), אותו
  מנגנון PointsTransaction הקיים (עם נעילת `SELECT ... FOR UPDATE`, כמו כל
  מקום אחר שנוגע ביתרה).
- **"לומדה" (הסבר לומד):** לכל שאלה יש שדה `explanation` **חובה** — כשהילד
  טועה, מוצג לו הסבר על הנושא כדי שיוכל ללמוד לבד. המשתמש הדגיש שזה
  **משמעותי מאוד**, לא תוספת שולית. ✅ נבנה קצה לקצה: ה-API מחזיר
  (`explanation`+`correctOptionIndex`) ומסך התרגול ב-Flutter מציג אותו
  בכרטיסיה כתומה עם 💡 מיד אחרי תשובה שגויה.
- להורה יש אפשרות לעדכן ידנית גם רמה/כיתה וגם ניקוד — הניקוד הידני כבר קיים
  (תנועות נקודות מרובד 1). ✅ הרמה/כיתה הידנית **נבנתה עכשיו קצה-לקצה**:
  `PATCH /children/:childId/subject-progress/:subject` +
  [subject_progress_screen.dart](app/lib/screens/subject_progress_screen.dart)
  (נגיש מתפריט ⋮ במסך ילד בודד → "רמת לימוד ותרגול").

**מחברת טיוטה לחישובים:** במקצוע חשבון (ורק שם) — אזור טקסט חופשי ליד השאלה
שהילד יכול "לשרבט" בו חישובים לפני שעונה (כמו מחברת בכיתה). לא נשמר, לא
נבדק — רק עזר ויזואלי לילד. ✅ נבנה ב-[practice_screen.dart](app/lib/screens/practice_screen.dart)
(מופיע רק כשהמקצוע הוא "חשבון").

### ✅ Flutter: מסכי התרגול לילד נבנו קצה-לקצה (2026-08-27)

- [practice_pin_screen.dart](app/lib/screens/practice_pin_screen.dart) — הזנת
  קוד PIN בן 4 ספרות (שדה מוסתר, מקלדת מספרים בלבד).
- [practice_subject_screen.dart](app/lib/screens/practice_subject_screen.dart)
  — בחירת מקצוע (חשבון/אנגלית/עברית).
- [practice_screen.dart](app/lib/screens/practice_screen.dart) — השאלה עצמה
  עם 4 תשובות, צ'יפים שמציגים כיתה+רמת קושי נוכחיים, מחברת טיוטה (חשבון
  בלבד), משוב ירוק/כתום מיידי, מונה נקודות לסבב הנוכחי, וכפתור "שאלה הבאה".
- ב-[child_detail_screen.dart](app/lib/screens/child_detail_screen.dart) נוסף
  כפתור "התחלת תרגול" ב-AppBar — אם לילד עוד אין PIN, נפתחת קודם דיאלוג
  הגדרת PIN (חד-פעמי), ואז נכנסים ישר למסך בחירת מקצוע.
- `flutter analyze` ו-`flutter test` נקיים, וגם `flutter build apk --debug`
  **עבר בהצלחה** (317 שניות, `app-debug.apk` נבנה) — כלומר כל הקוד החדש הזה
  קומפילציה תקינה לטלפון אנדרואיד בפועל, לא רק ל-Web. **בדיקה ויזואלית
  בדפדפן לא בוצעה ע"י ה-agent** (מגבלת סביבה קיימת — ראה סעיף המשאבים למטה;
  ה-console log של האפליקציה בדפדפן כן אומת נקי משגיאות אחרי הטעינה, אבל לא
  נבדקה לחיצה בפועל על המסכים החדשים). **זה הדבר הכי חשוב לבדוק ידנית
  כשתתפנה.**

### ✅ שדרוג ארכיטקטורה: כיתות א-ח × 3 רמות בכל כיתה (2026-08-27, בעקבות תיקון מפורש של המשתמש)

**מה קרה:** הבנייה הראשונה של מנוע התרגול השתמשה ב-3 רמות קושי בלבד (ללא
תלות בכיתה), עם טווח גיל (`minAge`/`maxAge`) על כל שאלה. בבדיקה בפועל
(agent) התגלה שילד יכול "להיתקע" — לעלות רמות עד HARD ואז לקבל "אין שאלות
זמינות" כי לא היה לאן להמשיך. המשתמש תיקן מפורש:
> "רק 3 רמות?? אתה צריך לתת חומר לפי כיתות א עד ח כדי שלכל ילד יהיה את
> התרגול שלו ובתוך כל כיתה 3 רמות" (+ "בכל כיתה" + "אתה צריך להתחבר בשביל
> זה לתכנית משרד החינוך לכל מקצוע כדי שתגע בכלל החומרים")

**הפתרון שנבנה — זה בדיוק המענה למגבלה שהמשתמש ביקש לרשום לעתיד** ("אלגוריתם
שלא מאפשר לילד לחגוג על נושא קל... ילד כיתה ה' שמצליח בחשבון שיגיע לשאלות
כיתה ו'"): מבנה **8 כיתות (א-ח, כלומר 1-8) × 3 רמות קושי בתוך כל כיתה**.

- **DB:** `Question.gradeLevel` (1-8, במקום `minAge`/`maxAge`),
  `ChildSubjectProgress.currentGrade` (בנוסף ל-`currentDifficulty` הקיים).
  מיגרציה `20260827183000_add_grade_levels` הופעלה בהצלחה (מחקה גם את 45
  השאלות הישנות מבוססות-גיל שכבר לא תואמות את המבנה החדש).
- **לוגיקת מעבר בין כיתות** (ב-[practice.ts](backend/src/routes/practice.ts)):
  עלייה ברמה אחרי HARD מקפיצה לכיתה הבאה ברמת EASY (מקסימום כיתה ח');
  ירידה מתחת ל-EASY מורידה לכיתה הקודמת ברמת HARD (מינימום כיתה א'). כלומר
  ילד שממשיך להצליח **חוצה כיתות נומינליות כלפי מעלה**, בדיוק כמו שהמשתמש
  ביקש בדוגמה שלו.
- **נבדק בפועל קצה-לקצה** (משפחת/ילד בדיקה שנוצרו ונמחקו בסוף): איפוס ל-כיתה
  1/MEDIUM → 3 תשובות נכונות → MEDIUM→HARD (עדיין כיתה 1) → עוד 3 נכונות →
  קפיצה לכיתה 2/EASY בפועל. תשובה שגויה בכיתה 2/EASY → ירידה נכונה לכיתה
  1/HARD. גם `subjectProgress.ts` עודכן לחשוף ולקבל `currentGrade` (GET+PATCH).
  `tsc --noEmit` נקי.

**⚠️ הסתייגות לגבי "תכנית משרד החינוך" (חשוב להבין):** המשתמש ביקש שהתוכן
יתחבר בפועל לתכנית הלימודים הרשמית של משרד החינוך. **מה שנעשה בפועל:** חיפוש
אינטרנט ברמת-על (לא מסמכי תכנית לימודים רשמיים/מלאים) כדי לגבש רשימת נושאים
סבירה לכל כיתה ומקצוע (למשל: כיתה א' חשבון = חיבור/חיסור עד 10, כיתה ז' =
אלגברה בסיסית). על בסיס זה נכתב מאגר **פתיחה** של 72 שאלות (8 כיתות × 3 רמות
× 3 מקצועות, שאלה אחת בכל תא) — ראה [seedQuestions.ts](backend/prisma/seedQuestions.ts),
כבר הוזן ל-DB. **זה לא כיסוי מלא ומדויק של התכנית הרשמית** — זו התחלה סבירה
שנועדה לתת לילד תרגול בכל טווח הכיתות בלי "בורות" (מקומות בלי אף שאלה).
כיסוי עומק אמיתי ומדויק לתכנית הרשמית מתוכנן להיבנות בעתיד באמצעות תכונת
יצירת התוכן ב-AI (כבר סוכם כפיצ'ר פרימיום עתידי) — זה בדיוק המקרה הראשון
שלשמו הפיצ'ר הזה נועד.

## תיקון באג אבטחה חמור: token של ילד יכל לגשת לנתיבי הורה

**מה מצאתי (בבדיקת עצמי של מנוע התרגול, לפני שמשתמש אמיתי היה יכול להיפגע):**
כשבניתי את ה-token הנפרד לילד (`ChildTokenPayload`), גיליתי ש-`verifyAuthToken`
(הפונקציה שבודקת token של **הורה**) לא בדקה בפועל שזה אכן token של הורה —
היא רק פיענחה את ה-JWT ובדקה חתימה תקינה. מכיוון ששני סוגי ה-token חולקים
שדה `familyId`, **token של ילד עבר בהצלחה גם בנתיבים שאמורים להיות הורה-בלבד**
(אימתתי בפועל עם curl: `GET /children` עם token של ילד החזיר 200 עם נתונים
אמיתיים של המשפחה — ילד יכול היה לראות/לערוך פרסים, משימות, תנועות נקודות
של כל האחים שלו).

**התיקון:** הוספתי שדה `type: "parent"` / `type: "child"` לתוך שני סוגי
ה-token, ו-[jwt.ts](backend/src/auth/jwt.ts) בודק בפועל את השדה הזה
ב-`verifyAuthToken`/`verifyChildToken` (זורק שגיאה אם type לא מתאים). אימתתי
עם curl בשני הכיוונים: token ילד על נתיב הורה → 401, token הורה על נתיב ילד
→ 401.

**⚠️ תופעת לוואי שחשוב שתדע:** כל token הורה שהונפק **לפני** התיקון הזה
כבר לא תקף (חסר לו שדה `type`) — **תצטרך להתחבר מחדש בפעם הבאה שתיכנס
לאפליקציה** (גם בדפדפן וגם באנדרואיד, אם היה session פעיל שם). זו לא תקלה —
זו תוצאה ישירה של תיקון אבטחה אמיתי.

## ⚠️ מגבלת משאבים על המחשב — עובדים איתה, לא מולה

**המחשב עמוס בזיכרון** (לפעמים יורד ל-1.5-2GB פנויים מתוך 15.7GB). זו הסיבה
לכל אי-היציבות של האמולטור לאורך השיחה. **החלטה:** ויתרנו על בדיקה חיה
באמולטור (המשתמש אישר) — **בדיקה חזותית תמיד דרך הדפדפן**
(http://localhost:8090), אבל **כל שינוי עדיין נבדק שהוא מתקמפל לאנדרואיד**
דרך `flutter build apk --debug` (לא צריך אמולטור בשביל זה, רק בודק שהקוד תקין
ובר-קימפול לטלפון בפועל).

**כדי לא לקרוס:** לא מריצים תהליכים כבדים (build אנדרואיד + Flutter Web +
בדיקות) **בו-זמנית** — אחד אחרי השני, ובודקים זיכרון פנוי (`Get-CimInstance
Win32_OperatingSystem`) לפני משימה כבדה. מנקים תהליכי Gradle/Dart ישנים
שנשארים ברקע אחרי כל build (`java`/`dartaotruntime`, בטוחים למחיקה — Gradle
פשוט יתחיל daemon חדש בבנייה הבאה).

## ✅ Git: כל הקוד עכשיו ב-commit (2026-08-28)

הקומיט הראשוני `3f27c7d` הכיל רק `.gitignore`+`README.md`. **המשתמש אישר
מפורשות בבוקר** ("יאללה תמשיך לרוץ קדימה") אחרי ששאלתי, אז ביצעתי commit
אחד עם כל מה שנבנה עד כה (`app/`, `backend/`, `CLAUDE.md` — 189 קבצים).
נבדק לפני ה-commit ש-`.env` וקבצי סוד אחרים לא נכנסו (מכוסים ע"י `.gitignore`
בכל רמה). **עדיין לא בוצע push** — אין remote מוגדר עדיין ב-repo המקומי.

## ✅ תיקון UX: ה"לומדה" נפתחת רק לפי בקשת הילד (2026-08-28)

**מהמשתמש:** "הלומדה תיפתח רק שהילד לא יצליח את השאלה וילחץ על הלומדה כדי
שתסביר לו איך לענות - ברגע שהילד יודע איך עונים הוא לא יפתח את הלומדה."

**התיקון** ב-[practice_screen.dart](app/lib/screens/practice_screen.dart):
אחרי תשובה שגויה, במקום להציג את ההסבר אוטומטית, מוצג כרטיס "לא נכון הפעם"
עם כפתור "לומדה — תסבירו לי". ההסבר עצמו מוצג רק אחרי לחיצה מפורשת של הילד
על הכפתור. `flutter analyze`+`flutter test` נקיים.

## ✅ יצירת שאלות עם AI — נבנה קצה-לקצה, ממתין למפתח API (2026-08-28)

הפיצ'ר הפרימיום שסוכם מראש ("AI ליצירת שאלות = עתידי, פרימיום") — נבנה עכשיו.

**החלטת ארכיטקטורה שקיבלתי (לא נדונה איתך במפורש, תרגיש חופשי לערער):** שאלות
שנוצרות ע"י AI הן **פרטיות למשפחה שיצרה אותן**, לא נכנסות למאגר הגלובלי
המשותף לכל המשפחות. זה הגיוני כי ההורה יוצר תוכן כדי לחזק נושא ספציפי
*לילד שלו*, לא כדי לתרום למאגר הציבורי. הרחבתי את `Question` עם `familyId`
אופציונלי (null = מאגר גלובלי/seed, לא-null = פרטי למשפחה) ו-`approved`
(ברירת מחדל true לתוכן seed, false לתוכן AI עד אישור הורה). ילד רואה גם
שאלות גלובליות וגם שאלות מאושרות של המשפחה שלו, לא של משפחות אחרות.

**זרימת העבודה:**
1. הורה בוחר מקצוע+כיתה+רמת קושי+כמות (1-10) במסך חדש
   [ai_content_screen.dart](app/lib/screens/ai_content_screen.dart) (נגיש
   מ-AppBar ב"הילדים שלי", אייקון ✨).
2. `POST /question-drafts/generate` שולח פרומפט ל-Claude (מודל `claude-sonnet-5`
   דרך `@anthropic-ai/sdk`) ומבקש JSON טהור של שאלות רב-ברירה עם הסבר. הפרומפט
   כולל את כל הכללים (4 תשובות, הסבר שמלמד לא רק "התשובה היא X", תוכן מתאים
   לגיל). התשובה נבדקת/מאומתת (parseGeneratedQuestions ב-
   [questionDrafts.ts](backend/src/routes/questionDrafts.ts)) לפני שמירה ב-DB.
3. השאלות נשמרות עם `approved: false` — **לא מוצגות לילד עדיין**.
4. ההורה רואה רשימת "שאלות ממתינות לאישור" (עם התשובה הנכונה מסומנת + ההסבר),
   ויכול לאשר או לדחות כל שאלה בנפרד. רק אחרי אישור היא נכנסת לתרגול בפועל.

**✅ מוגדר ונבדק בפועל (2026-08-28):** המשתמש יצר חשבון+מפתח ב-
console.anthropic.com (קטגוריית "אישי", $5 קרדיט התחלתי) והביא לי את המפתח.
הכנסתי אותו ל-`backend/.env` (לא נכנס ל-git, מוגן ע"י `.gitignore`), הפעלתי
מחדש את שרת ה-backend כדי שיטען את המשתנה, ובדקתי עם קריאה אמיתית: ביקשתי
2 שאלות חשבון כיתה ג' רמה בינונית — Claude החזיר שאלות מילוליות תקינות
בעברית עם חשבון נכון (245-118=127, 6×7=42) והסברים טובים. בדקתי גם את זרימת
האישור (PATCH approved:true) — עובד. כל נתוני הבדיקה נמחקו מה-DB בסיום.

**נבדק גם:** `tsc --noEmit` נקי, מיגרציה `20260828090000_add_ai_generated_questions`
הופעלה בהצלחה, ושילד עדיין מקבל שאלות תקינות מהמאגר הגלובלי (אין רגרסיה).
`flutter analyze`+`flutter test`+`flutter build apk --debug` כולם עברו.
הוספתי גם טאב "כבר אושרו" במסך ה-AI כדי שההורה יוכל לראות/להסיר שאלות AI
שכבר אושרו, לא רק לטפל בממתינות.

**מצב: הפיצ'ר הזה שלם ופעיל.** הורה יכול היום ליצור שאלות AI אמיתיות, לאשר
אותן, והן מיד זמינות לתרגול הילדים שלו.

**Git:** שני commits חדשים נוספו הבוקר (עם אישור מפורש של המשתמש): אחד עם כל
הבסיס (Tier 1 + מנוע התרגול), אחד עם פיצ'ר ה-AI. **עדיין לא בוצע push** —
יש `origin` מוגדר (github.com/yairbd1-code/kids-learning-app) אבל לא דחפתי
בלי לשאול (פעולה שמשפיעה על מקום חיצוני/משותף).

## תיקון באג: ילד חדש התחיל תמיד מכיתה א' (2026-08-28)

**מה המשתמש דיווח (מבדיקה ויזואלית אמיתית באתר):** "הגדרתי ילדה כיתה ו וגיל
11 - השאלות שלה לא תואמות בכלל - קיבלה שאלות לילד בכיתה א". הסיבה: כל
`ChildSubjectProgress` חדש נוצר עם `currentGrade` ברירת מחדל 1 מהסכמה, בלי
קשר לגיל/כיתה האמיתיים של הילד.

**התיקון:** הוספתי ב-[practice.ts](backend/src/routes/practice.ts) פונקציה
`defaultGradeForAge(age)` — כיתה א' בישראל מתחילה בערך בגיל 6, אז כיתה
משוערת = `גיל - 5` (מוגבל 1-8). כשנוצרת רשומת התקדמות ראשונה לילד+מקצוע,
היא מתחילה מהכיתה הזו במקום מכיתה 1 קבועה. **וידאתי שהנוסחה מדויקת** מול
שלושת הילדים האמיתיים של המשתמש: דביר (9, כיתה ד') → 4 ✅, עוז (7, כיתה ב')
→ 2 ✅, אילה (11, כיתה ו') → 6 ✅. **תיקנתי גם ישירות ב-DB** את שלוש רשומות
ההתקדמות הקיימות של אילה (היו כבר תקועות בכיתה 1) לכיתה 6.

## שישה פיצ'רים חדשים ל"ניהול הורים" — כולם בנויים ונבדקו קצה-לקצה (2026-08-28)

לבקשת המשתמש ("להוסיף ניהול להורים... שני משתמשים לנהל אבא ואמא... שינוי
קוד, הוספת שאלות, מעקב התקדמות, חסימת/שילוב תחומים"):

1. **חסימת מקצוע לילד** — `Child.disabledSubjects String[]`. הורה מכבה
   מקצוע במסך "רמת לימוד ותרגול" (מתג לכל מקצוע) — הוא נעלם לגמרי ממסך
   בחירת המקצוע של הילד. נאכף גם ב-`next-question` וגם ב-`answer` (הגנה
   כפולה, לא רק ב-UI).
2. **הוספת שאלה ידנית** — `POST /question-drafts/manual` (מקור `MANUAL`,
   מאושר מיידית — ההורה כתב אותה בעצמו, אין צורך בסבב אישור). כפתור ✏️
   במסך "יצירת שאלות עם AI".
3. **מעקב התקדמות** — `GET /children/:id/practice-stats` — לכל מקצוע: כמה
   שאלות נענו, אחוז הצלחה. מוצג במסך "רמת לימוד ותרגול".
4. **תרגול מעורב עם אחוזים** — הורה קובע אחוזים לכל מקצוע (סליידרים),
   ולילד יש אפשרות "תרגול מעורב" שמערבב שאלות מכל המקצועות הפעילים לפי
   האחוזים (`GET /practice/mixed/next-question`, עם fallback לשאר המקצועות
   אם לזה שנבחר אין כרגע שאלות ברמה). **נבדק:** עם משקל 90% לחשבון קיבלתי
   9 מתוך 10 שאלות חשבון.
5. **שני הורים לאותה משפחה** — כל משפחה מקבלת **קוד משפחה** בן 6 תווים
   (`Family.joinCode`, ייחודי, ללא תווים דו-משמעיים כמו 0/O). הורה שני
   נרשם עם "הצטרפות למשפחה קיימת" + הקוד → מצטרף לאותה משפחה, רואה את אותם
   ילדים. הקוד מוצג במסך ההגדרות (עם כפתור העתקה) יחד עם רשימת ההורים
   הקיימים. **נבדק בפועל:** שני הורים עם טוקנים נפרדים רואים את אותו ילד.
6. **פרופיל "ספרי לימוד לשנה" + OCR** — מסך חדש לכל ילד (מתפריט ⋮ במסך
   הילד) שבו ההורה מוסיף תיאור של החומר הנלמד השנה — או בכתיבה חופשית, או
   בצילום/העלאת תמונה של דף מהספר. **התמונה נשלחת ל-Claude (vision) לקריאה
   וסיכום, ונמחקת מיד אחרי — נשמר רק הטקסט שחולץ ממנה**, בדיוק כמו שביקשת.
   במסך "יצירת שאלות עם AI" יש עכשיו שדה נוסף "התאמה לחומר הלימוד של: [ילד]"
   — כשנבחר ילד, השאלות הבאות מתחשבות בחומר הספציפי שלו, לא רק ברמת הכיתה
   הכללית. **נבדק בפועל עם תמונה סינתטית אמיתית** (טקסט "Unit 5: Present
   Simple Tense" וכו') — Claude קרא אותה נכון וסיכם בעברית מדויקת. **נבדק
   גם** שיצירת שאלות עם `childId` מחזירה שאלות שמתאימות בדיוק לחומר שהוזן
   (שאלת שברים/כפל דו-ספרתי אחרי שהוזן "כפל וחילוק דו-ספרתי, שברים").

**תלות טכנית חדשה:** נוספה חבילת `image_picker` ל-Flutter (מצלמה+גלריה),
ונוספה הרשאת `CAMERA` ל-AndroidManifest.

**נבדק לאורך כל הפיתוח:** `tsc --noEmit`, `flutter analyze`, `flutter test`,
ו-`flutter build apk --debug` כולם עברו נקי (כמה תקלות זמניות תוקנו תוך
כדי: שגיאת type אחרי עריכה במקביל לבנייה, אזהרת lint על תחביר null-aware
מפה `{'key': ?value}`). כל נתוני הבדיקה נוקו מה-DB אחרי כל בדיקה.

## חנות: הילד מבקש פרס, ההורה מאשר/דוחה (2026-08-28)

לבקשת המשתמש: "עשה שהחנות תעבוד כמו מנוע התרגול — שהילד יעשה את הכל לבד",
עם הבהרה חשובה **ששונה ממנוע התרגול**: תשובה בתרגול נבדקת אוטומטית ע"י
המערכת, אבל מימוש פרס אמיתי (למשל "20 דקות מסך" או "גלידה") תלוי בהורה
שבאמת ייתן את זה בעולם האמיתי — אז נשמרה נקודת בקרה אנושית.

**הזרימה החדשה:**
- לילד יש עכשיו מסך בית אחרי כניסה עם PIN (`ChildHomeScreen`) עם שתי
  אפשרויות: "תרגול" (כמו קודם) ו-"חנות פרסים" (חדש).
- במסך החנות (`ChildStoreScreen`) הילד רואה את היתרה שלו, את כל הפרסים,
  ולוחץ "בקשה" על פרס — **לא מקזז נקודות מיד**, רק יוצר בקשה בסטטוס
  `PENDING`. יש גם טאב "הבקשות שלי" שמראה סטטוס (ממתין/אושר/נדחה).
- ההורה רואה במסך הילד פעמון עם מספר הבקשות הממתינות, לוחץ ורואה רשימה עם
  כפתורי ✓/✗ לכל בקשה. **רק כשההורה מאשר, הנקודות מקוזזות בפועל.**
- הזרימה הישנה (הורה מממש ישירות עבור הילד, "מימוש פרס") **נשארה כמו
  שהייתה** — עדיין שימושית כשההורה רוצה לתת פרס באופן מיידי בלי לחכות
  לבקשה מהילד.

**שינוי סכמה:** `Redemption` קיבל `status` (PENDING/APPROVED/REJECTED,
ברירת מחדל APPROVED לשמירת תאימות לאחור), ו-`pointsTransactionId` הפך
לאופציונלי (null כשעוד לא קוזזו נקודות). נתיב חדש `/store/*` (מוגן
`requireChildAuth`, כמו `/practice/*`) עם `balance`, `rewards`,
`redemptions` (GET+POST). נתיב חדש להורה: `PATCH
/children/:id/redemptions/:id` לאישור/דחייה.

**נבדק בפועל בקצה-לקצה עם curl:** ילד עם 100 נקודות ביקש פרס ב-30 —
היתרה נשארה 100 (לא קוזז כלום). ההורה אישר — היתרה ירדה ל-70 בדיוק. בקשה
שנייה שההורה דחה — היתרה נשארה 70 (לא השתנתה). `tsc --noEmit`,
`flutter analyze`, `flutter test`, ו-`flutter build apk --debug` כולם
עברו נקי.

**⚠️ תקלת סביבה לא-קשורה שנתקלתי בה תוך כדי:** שרת ה-Web (`flutter run`)
קרס עם "Dart compiler exited unexpectedly" — כנראה לחץ זיכרון אחרי שעות
של hot-reload מצטבר. ניקיתי תהליכי Gradle ישנים שתפסו כ-1.9GB (בטוח
לעשות, כמו תמיד), הפעלתי מחדש את שרת ה-Web, וזה עלה בסדר. אם זה קורה שוב
תוך כדי שאתה עובד — זה לא קשור לקוד, רק לזיכרון של המחשב.

## ⚠️ המחשב הנוכחי הגיע לגבול — המשתמש עובר למחשב חדש (2026-08-28)

בהמשך לתקלת הזיכרון למעלה: ניסינו גם אמולטור אנדרואיד והוא לא הצליח לעלות
כלל (אותה בעיית זיכרון, חמורה יותר כי אמולטור דורש עוד יותר RAM). המשתמש
החליט לקנות מחשב חדש. **המלצתי (מבוססת על מה שראיתי בפועל רץ על המחשב
הזה — לא רק כלי פיתוח, גם דפדפן עם כמה טאבים, Spotify, WhatsApp, וכמה
סשנים של Claude יחד):** 32GB RAM (חובה), מעבד 8 ליבות מודרני (Intel i7
דור 12+ / AMD Ryzen 7), SSD NVMe 512GB-1TB, Windows 11.

**עד אז:** לא מנסים שוב אמולטור, ולא מריצים שרת Web/כלי Flutter בכלל
(אפילו `flutter analyze` הקליל) כשהזיכרון הפנוי מתחת ל-~2GB, כי זה בערך
תמיד נכשל. **ממשיכים לבנות רק בצד ה-backend**, נבדק ב-curl (לא צריך שום
כלי Flutter בשביל זה) — כך אפשר להתקדם גם עם המגבלה הזו.

## תמיכה בקבצים (לא רק תמונות) ל"ספרי לימוד" — backend מוכן, Flutter ממתין (2026-08-28)

בהמשך לבקשה המקורית ("ה-OCR יתמוך גם בהעלאת קובץ, לא רק צילום") — הרחבתי
את [curriculumNotes.ts](backend/src/routes/curriculumNotes.ts) לקבל גם
`mediaType: "application/pdf"` בנוסף לתמונות. Claude תומך ב-PDF ישירות
(content block מסוג `document`, לא רק `image`) — כך שלא היה צריך להוסיף
שירות המרת PDF-לתמונה בעצמנו. **נבדק בפועל:** יצרתי PDF אמיתי (לא מזויף)
עם הטקסט "Chapter 4: Multiplication Tables" ושלחתי אותו לנתיב — Claude
קרא אותו נכון והחזיר תקציר עברי מפורט + הצעות לשאלות. שדה `source` במאגר
מבחין בין `photo`/`file`.

**מה שנשאר:** צד ה-Flutter (מסך "ספרי לימוד לשנה") עדיין תומך רק בתמונה
(מצלמה/גלריה) — צריך להוסיף כפתור "העלאת קובץ" עם בורר קבצים (למשל חבילת
`file_selector` או `file_picker`) שיודע לשלוח גם PDF. **לא בניתי את זה
עדיין** כי אין לי דרך לבדוק ויזואלית כרגע (לא Web, לא אמולטור) — ממתין
לזיכרון פנוי/מחשב חדש כדי להמשיך משם בבטחה.

## עיצוב (UI/UX) + פיצ'ר: תמונת פרופיל וצבע אישי לילד — בנוי קצה-לקצה (2026-08-28)

**חשוב לרקע:** תכנון העיצוב הזה (ולוח המוקאפים) נעשה בטעות בתחילה בסשן
שעבד בתיקייה הישנה `בניית אפליקציות\kids-learning-app` (שהתברר שריקה/לא
מחוברת ל-git — בדיוק התיקייה שכבר מסומנת למטה כמיועדת למחיקה אופציונלית).
ברגע שהתגלה הבלבול, המשכנו לעבוד כאן, בתיקייה האמיתית, ומימשנו הכל מכאן.

**שפה עיצובית שסוכמה** (לוח מוקאפים ב-Claude Design — הקישור נמצא בהיסטוריית
הצ'אט של אותה שיחה):
- צבעוני ומשחקי, גופן Rubik, פינות מעוגלות, כרטיסים/כפתורים בצורת pill.
- צבע מותג קבוע (כחול-אינדיגו, `#0075A9`) למסכי הורה/ניהול.
- ערכת צבע אישית לכל ילד/ה — 6 אפשרויות קבועות: אוקיינוס, פריחה, יער,
  גלקסיה, שמש, אש. לא מסומנות כ"לבנים"/"לבנות" בכוונה — כל ילד/ה בוחר/ת מה
  שמדבר אליו/ה. ה-hex המדויק לכל ערכה מחושב מ-oklch המקורי ומוגדר ב-
  [child_theme.dart](app/lib/theme/child_theme.dart).
- תמונת פרופיל אופציונלית; בלי תמונה מוצגות אותיות ראשונות על רקע ערכת
  הצבע ([child_avatar.dart](app/lib/widgets/child_avatar.dart)).

**מה נבנה בפועל:**
- **DB:** שני שדות חדשים ל-`Child` — `photoUrl` (nullable) ו-`themeId`
  (ברירת מחדל `"ocean"`). מיגרציה `add_child_photo_theme` הורצה ויושמה.
- **Backend** ([children.ts](backend/src/routes/children.ts)): `POST`/`PATCH`
  מקבלים ומאמתים `photoUrl` (data URI של jpeg/png/webp, עד ~3MB) ו-`themeId`
  (חייב להיות אחד מ-6 הערכים). `serializeChild` מחזיר את שניהם.
- **תיקון תשתית נלווה:** הגבלת גודל ה-body הגלובלית ב-
  [index.ts](backend/src/index.ts) הייתה 100kb (ברירת המחדל של
  `express.json()`) — קטנה מדי לתמונות base64. הגדלתי ל-10mb גלובלית. שווה
  לדעת: זה כנראה תיקן גם תקלה סמויה זהה בהעלאת תמונות ל"ספרי לימוד"
  (ה-parser הייעודי שם ב-curriculumNotes.ts לא היה נתפס בפועל לפני זה, כי
  ה-parser הגלובלי כבר היה דוחה בקשות גדולות קודם).
- **Flutter:** `Child` מכיל את שני השדות; ווידג'טים חדשים משותפים -
  `ChildAvatar` (תצוגה) ו-`ChildAppearancePicker` (בחירת תמונה דרך
  `image_picker`, מכווץ ל-480x480/איכות 75, + 6 עיגולי צבע לבחירה) - בשימוש
  גם בדיאלוג "הוספת ילד" וגם ב"עריכת פרטי ילד" ב-
  [children_screen.dart](app/lib/screens/children_screen.dart) ו-
  [child_detail_screen.dart](app/lib/screens/child_detail_screen.dart).
  מסך הילד הבודד צבוע בערכת הצבע האישית שלו (top bar + כרטיס יתרה).

**נבדק:** מיגרציה + `POST`/`PATCH` נבדקו ב-curl (כולל ולידציה - themeId/
photoUrl לא תקינים מחזירים 400) על שרת טרי, ונוקו נתוני הבדיקה. `flutter
analyze` ו-`flutter test` רצו נקי (בניגוד להערה למטה על מגבלת הזיכרון -
הפעם זה עבד בלי בעיה). **עדיין לא נבדק ויזואלית ע"י המשתמש בדפדפן/מכשיר.**

## ✅ מסך "הצבעים שלי" — הילד בוחר את הצבע שלו בעצמו (2026-08-28)

השלמתי את הפער שנרשם למעלה: עכשיו שלילד יש session משלו (PIN), הבחירה כבר
לא צריכה לעבור דרך ההורה — בדיוק כמו התכנון המקורי.

- **Backend:** נתיב חדש `/me` (מוגן `requireChildAuth`, כמו `/practice`
  ו-`/store`) — `GET` מחזיר את הפרופיל של הילד המחובר (שם, themeId, photoUrl),
  `PATCH` מאפשר לו לשנות רק את ה-`themeId` שלו (לא תמונת פרופיל — זו נשארת
  בשליטת ההורה, לשמירה על פיקוח). ראה [childProfile.ts](backend/src/routes/childProfile.ts).
- **Flutter:** מסך חדש [child_theme_screen.dart](app/lib/screens/child_theme_screen.dart)
  — 6 ריבועי צבע גדולים עם gradient, נגיש מכפתור פלטה ב-`ChildHomeScreen`.
  הצבע שנבחר מוחל מיד על ה-AppBar של מסך הבית, מסך בחירת המקצוע, ומסך
  החנות — כך שכל session של הילד "צבוע" בצבע האישי שלו/ה.
- **נבדק ב-curl:** ילד רואה את הצבע שלו (ברירת מחדל `ocean`), משנה ל-`fire`
  בהצלחה; טוקן הורה מקבל 401 בניסיון גישה ל-`/me` (זה רק לילד); ערך צבע לא
  תקין מוחזר עם 400 ברור. `tsc --noEmit`, `flutter analyze`, `flutter test`
  עברו נקי.

## ✅ דיפלוי: האפליקציה חיה באינטרנט (2026-08-30)

**מה נבנה:** backend + Postgres רצים ב-Render (תוכנית חינמית), דרך
[render.yaml](render.yaml) (Blueprint — מגדיר את שני השירותים ביחד).
`buildCommand` מתקין תלויות, מריץ `prisma generate` ו-`npm run build`;
`startCommand` מריץ `prisma migrate deploy` (מיישם מיגרציות אוטומטית בכל
דיפלוי) ואז מפעיל את השרת. הסודות (`GOOGLE_CLIENT_ID`, `ANTHROPIC_API_KEY`)
מוזנים ידנית בלוח הבקרה של Render (`sync: false`), `JWT_SECRET` נוצר
אוטומטית, `DATABASE_URL` מחובר אוטומטית ל-DB.

**כתובת חיה:** https://kids-learning-backend-l2ts.onrender.com — נבדק בפועל
מול ה-DB האמיתי (הרשמה אמיתית הצליחה, נתוני הבדיקה נוקו אחר כך לפי ID מדויק).

**אפליקציית ה-Flutter Web מוגשת מאותו שרת, מאותה כתובת** — כדי לא להקים
שירות Render נפרד רק בשביל ה-frontend. `flutter build web
--dart-define=API_BASE_URL=https://kids-learning-backend-l2ts.onrender.com`
בונה חבילה עם כתובת השרת ה"אמיתית" קבועה בקוד המקומפל (ראה
[auth_service.dart](app/lib/services/auth_service.dart) — `String.fromEnvironment`,
ריק כברירת מחדל כדי לא להשפיע על `flutter run` הרגיל בפיתוח). התוצאה
מועתקת ל-`backend/public/` ומוגשת סטטית ע"י Express
([index.ts](backend/src/index.ts) — `express.static` + נפילה ל-`index.html`
לכל בקשת GET שלא תואמת נתיב API ידוע, בשביל ניווט בתוך האפליקציה).

**⚠️ מגבלה חשובה שצריך לזכור:** ל-Render אין Flutter SDK מותקן, אז אי אפשר
לבנות את ה-Web build אצלם. **`backend/public/` הוא build מוכן, מחושב פעם
אחת מקומית ונשמר ב-git** (לא נבנה מחדש אוטומטית). המשמעות: **כל שינוי
ב-Flutter (מסך, טקסט, לוגיקה) לא יופיע באתר החי עד שמישהו/מה יריץ שוב
`flutter build web --dart-define=...` מקומית, יעתיק ל-`backend/public/`,
ויעשה commit+push.** זה תהליך ידני בשלב הזה — שווה לזכור את זה ולבקש ממני
לעשות את זה מפורש אחרי כל סבב שינויים ב-Flutter שרוצים לראות באתר החי (לא
רק בפיתוח מקומי).

**נבדק:** `flutter analyze`+`flutter test` נקיים, ה-build המקומי הצליח
(`main.dart.js` נוצר, נבדק שכתובת ה-backend הנכונה קבועה בתוכו), והשרת
המקומי הגיש את הקבצים נכון (`curl localhost:3000/` החזיר את ה-HTML, קובץ
ה-JS נגיש). **עדיין לא נבדק ע"י המשתמש בדפדפן אמיתי מול הכתובת החיה** —
ה-agent לא יכול לבדוק חזותית בסביבה הזו (כמו תמיד השיחה), ובכל מקרה
המשתמש ביקש לדחות בדיקה חזותית עד שיגיע מחשב חדש.

## שאלות פתוחות למשתמש (לענות כשנוח — לא חוסם המשך עבודה)

1. **בדיקה ויזואלית בדפדפן** — המשתמש **התחיל** לבדוק בפועל (ומצא את באג
   הכיתה למעלה!) — ממשיכים משם. עדיין כדאי לעבור על שאר הפיצ'רים החדשים
   (תרגול מעורב, חסימת מקצוע, ספרי לימוד+OCR, שני הורים).
2. **מאגר 72 השאלות הבסיסי** — עדיין רק שאלה אחת לכל שילוב מקצוע+כיתה+רמה,
   כך שילד שמתרגל הרבה יראה חזרות. עכשיו שיש AI ליצירת שאלות פעיל, הפתרון
   הכי מהיר הוא ליצור עוד שאלות עם זה לפי הצורך.
3. **OCR** — כפי שביקשת, יעודכן בעתיד לתמוך גם בצילום עמודים מתוך הספר
   עצמו (לא רק רשימת נושאים) ובהעלאת קבצים, כדי לתרגל ישירות מהחומר הספציפי
   — **גרסה ראשונה כבר בנויה ועובדת** (תמונה בודדת → תיאור טקסטואלי → הקשר
   ליצירת שאלות), ההרחבה הזו (קבצים, ריבוי עמודים) תהיה השלב הבא כשתרצה.

## פעולות שדורשות אותך (לא ניתנות לביצוע ע"י ה-agent)
- **בדיקה ויזואלית** — כשתתפנה (המשתמש ביקש לדחות עד למחשב חדש): באתר החי
  https://kids-learning-backend-l2ts.onrender.com **או** מקומית ב-
  http://localhost:8090 (אם השרת המקומי רץ ברקע). האמולטור לא בשימוש כרגע
  (מגבלת זיכרון, ראו למעלה).
- **Render:** אם דיפלוי לא עולה אוטומטית אחרי `git push` (autoDeploy אמור
  לעבוד, אבל לא אומת) — צריך "Manual Sync" ידני בלוח הבקרה של Render.
- אופציונלי, לא דחוף: מחיקת התיקייה הישנה `בניית אפליקציות\kids-learning-app`,
  וסגירת תוכנות לא נחוצות (PowerPoint, טאבי כרום מיותרים) כדי לפנות זיכרון
  אם רוצים לחזור לאמולטור בעתיד.
- כדאי לזכור (לא דחוף): ה-DB החינמי ב-Render נמחק אוטומטית אחרי כמה חודשים
  אם לא משדרגים לתוכנית בתשלום (~$7/חודש) — הוחלט להתחיל בחינמי ולחזור לזה
  מאוחר יותר.
